'use client';

import { useState, useEffect, useRef } from "react";

export const Cursor = () => {
  // The cursor animation has been removed as requested.
  // The component now returns null, effectively disabling it.
  return null;
};